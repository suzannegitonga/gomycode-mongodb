# MongoDB CRUD Checkpoint
This project demonstrates basic MongoDB CRUD operations using the Mongo Shell.
Includes:
- Database & Collection creation
- Inserting documents
- Querying with filters ($gt, $lt, regex)
- Updating and Deleting records